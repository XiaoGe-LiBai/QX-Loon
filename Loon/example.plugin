/*
name: 示例插件（Loon）
version: 0.1.0
author: XiaoGe-LiBai
description: 演示 Loon 插件如何调用 Task 中的共享函数。
*/

(async function() {
  const params = { enabled: true, message: "hello from example.plugin" };

  if (typeof globalThis.exampleTaskRun === "function") {
    const result = await globalThis.exampleTaskRun(params);
    console.log("[example.plugin] result:", result);
  } else {
    console.warn("[example.plugin] 未检测到 exampleTaskRun（globalThis），请先加载 Task/example-task.js。");
  }
})();